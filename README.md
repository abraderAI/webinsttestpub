# webinsttestpub

Public DEFTless fixture for webinstaller /install/run testing

This repo is a synthetic fixture for testing the DEFTai webinstaller's
`/install/run` flow. It is intentionally **not** DEFTai-managed
(no `.deft/` directory, no `AGENTS.md` trampoline) so the install
flow has work to do when pointed at it.

Generated 2026-04-28T04:30:25Z.
